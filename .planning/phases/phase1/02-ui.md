---
phase: 1
plan: 02-ui
name: UI Enforcement & ViewModel
goal: Reflect strict log policies in the UI by disabling actions and adding server-side route guards.
depends_on: ["01-logic"]
must_haves:
  truths:
    - name: ui_action_visibility
      description: Action buttons (edit/delete) must be hidden or disabled for logs that cannot be modified.
  artifacts:
    - path: src/hopaoems/blueprints/ui_production.py
      provides: Route-level status validation.
      min_lines: 200
  key_links:
    - from: ui_production.py
      to: production/view.html
      via: is_editable flag
---

# Plan 02-ui: UI Enforcement & ViewModel

<task id="2.1" name="Update production routes guards" status="todo">
<files>
  <file>src/hopaoems/blueprints/ui_production.py</file>
</files>
<action>
In `production_log_edit` and `production_log_delete`, add initial checks using repo functions to block access if the log status is incompatible.
</action>
<verify>
<automated>python -m pytest tests/test_production_mvp.py</automated>
Try to access edit/delete routes for a washed log and expect 403 or redirect with error.
</verify>
<done>
Routes are protected against invalid status modifications.
</done>
</task>

<task id="2.2" name="Enhance ViewModel with flags" status="todo">
<files>
  <file>src/hopaoems/blueprints/ui_production.py</file>
</files>
<action>
Update the log list logic (likely in `production_task_view`) to attach `is_editable` and `is_deletable` boolean flags to each log object.
</action>
<verify>
<automated>python -m pytest tests/test_production_mvp.py</automated>
Check that unwashed logs have flags set to True, and washed logs have them set to False.
</verify>
<done>
Log objects in the view model contain necessary UI state flags.
</done>
</task>

<task id="2.3" name="Update log table actions" status="todo">
<files>
  <file>src/hopaoems/templates/production/view.html</file>
</files>
<action>
Update the `production/view.html` template to conditionally render or disable edit/delete action links based on the new flags.
</action>
<verify>
<automated>python -m hopaoems.contracts.run_gates</automated>
Visually verify buttons are hidden/disabled for washed logs in the task view.
</verify>
<done>
UI accurately reflects log editability.
</done>
</task>
